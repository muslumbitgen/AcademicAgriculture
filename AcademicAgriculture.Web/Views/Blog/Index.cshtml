@model ICollection<AcademicAgriculture.Entities.Concrete.Blog>

@{
    ViewData["Page"] = "Haber Listesi";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header">
        <a class="btn btn-primary btn-sm btn-labeled mr-5" asp-controller="Blog" asp-action="Add">
            <div class="hidden-xs hidden-sm p0-10">
                <span class="btn-label">
                    <span class="icon icon-plus icon-lg icon-fw"></span>
                </span>
                <span>Yeni</span>
            </div>
            <icon class="hidden-lg hidden-md btn icon icon-plus"></icon>
        </a>
    </div>
    <div class="card-body p-t-0">
        <div class="card-body">
            <table id="demo-datatables-colreorder-2" class="table table-hover table-striped table-bordered table-nowrap dataTable" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <td>işlem</td>
                        <td>Başlık</td>
                        <td>Durum</td>
                        <td>Resim</td>
                        <td>Açıklama</td>
                        <td>Oluşturma Tarihi</td>
                        <td>Güncelleme Tarihi</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var blog in Model)
                    {
                    <tr>
                        <td>
                            <a class="btn btn-xs btn-info" asp-controller="Blog" asp-action="Update" asp-route-id="@blog.Id"><icon class="icon icon-edit"></icon></a>
                            <a class="btn btn-xs btn-danger btndel" data-id="@blog.Id" data-controller="Blog" data-message="Haber kategorisi"><icon class="icon icon-trash"></icon></a>
                        </td>
                        <td>@blog.Title</td>
                        <td><button type="button" value="@blog.Id??Blog??IsActiveBlog" class="btn btn-@(blog.IsActive == true ? "success" : "danger") btn-sm">@(blog.IsActive == true ? "Aktif" : "Pasif")</button></td>
                        <td>
                            <img src="~/img/@blog.Url" width="80" />
                        </td>
                        <td>@blog.Description</td>
                        <td>@blog.CreateDate</td>
                        <td>@blog.UpdateDate</td>

                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>